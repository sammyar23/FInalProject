extends layout

block content
  h1 My Saved Builds
  ul.builds-list
    each build in builds
      li
        h2 Build Name: #{build.name}
        if build.amount
          p Total Amount: $#{build.amount}
        else
          p Total Amount: Calculation needed
